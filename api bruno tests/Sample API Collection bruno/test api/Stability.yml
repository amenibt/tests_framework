info:
  name: Stability
  type: http
  seq: 2

http:
  method: GET
  url: https://automationintesting.online/api/room
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("API stable", function () {
          expect(res.status).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
